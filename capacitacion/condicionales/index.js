// Condicionales

var nro1 = 40
var nro2 = 20

if (nro1 > nro2) {
    console.log(`el ${nro1} es mayor al ${nro2}`) //para que el mensaje muestre el contenido de la variable se tiene que usar las comillas inclinadas
} else {
    console.log(`el ${nro1} es menor al ${nro2}`)
}

// mostrar en cada mensaje a que nros nos estamos refiriendo